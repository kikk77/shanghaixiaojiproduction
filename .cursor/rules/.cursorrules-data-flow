数据流向规范：
后端(Railway) → API接口 → 前端展示

职责划分：
- 后端：处理Telegram API、数据库操作、业务逻辑
- 中间层：纯数据传输，不得修改数据结构
- 前端：根据后端数据格式进行UI展示

调试时必须遵循：
1. 使用Railway logs查看后端真实输出
2. 使用浏览器DevTools确认前端收到的数据
3. 数据不匹配时，修改前端代码适配后端格式
4. 绝不根据前端需求随意修改后端返回格式 