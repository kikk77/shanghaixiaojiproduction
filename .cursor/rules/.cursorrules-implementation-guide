当遇到数据显示问题时：

第一步：查看Railway logs
- 检查后端实际返回的数据结构
- 确认数据库查询结果
- 查看API响应格式

第二步：前端数据接收检查
- 使用console.log输出接收到的数据
- 对比后端logs和前端接收数据
- 确认数据传输过程中是否有变化

第三步：修改策略
- 如果数据结构不匹配：修改前端代码适配后端
- 如果数据内容错误：检查后端业务逻辑
- 如果传输过程有问题：检查中间层代码

禁止操作：
- 不得为了前端显示方便而修改后端数据结构
- 不得在中间层随意转换数据格式
- 不得根据前端期望格式修改API返回 